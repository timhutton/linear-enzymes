1. Install Emscripten:
    https://emscripten.org/
2. Compile:
    `build.cmd` (on Windows) or `sh build.cmd` (on Linux)
3. Start local webserver:
    python server.py
4. Open:
    http://localhost:8080
